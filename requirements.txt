# Runtime dependencies for the Listing Extractor API (pinned).
# Install: pip install -r requirements.txt
# Dev/test tools (pytest, ruff) live in pyproject.toml's [dev] extra.
httpx==0.28.1
pydantic==2.13.4
tenacity==9.1.4
python-dotenv==1.2.2
fastapi==0.136.3
uvicorn[standard]==0.49.0
